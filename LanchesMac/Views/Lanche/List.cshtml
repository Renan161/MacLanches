@model LancheListViewModel

<div><h2>@Model.CategoriaAtual</h2></div>

@{
    <div class="row">

        @foreach (var lanche in Model.Lanches)
        {
            <div class="col-sm-4 col-lg-4 col-md-4 ">

                <Partial name="_LanchesResumoView" for="@lanche" />

            </div>
        }
    </div>
    }
